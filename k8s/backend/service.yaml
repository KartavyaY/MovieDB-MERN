apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: moviedb
  labels:
    app: backend
spec:
  selector:
    app: backend
  ports:
    - name: http
      port: 5001
      targetPort: 5001
  # LoadBalancer on Docker Desktop binds to localhost:5001 automatically
  type: LoadBalancer
